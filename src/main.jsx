import React from 'react';
import ReactDOM from 'react-dom/client';
import WpmApp from './WpmApp';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<WpmApp />
	</React.StrictMode>
);
