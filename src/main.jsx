import React from 'react';
import ReactDOM from 'react-dom/client';
import TypingTestApp from './TypingTestApp';
import '../styles.css';

ReactDOM.createRoot(document.getElementById('root')).render(
	<React.StrictMode>
		<TypingTestApp />
	</React.StrictMode>
);
